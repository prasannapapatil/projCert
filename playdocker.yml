- hosts: webserver
  become: true
  become_user: root
  tasks:
  - name: Install Docker
    yum: 
      name: docker 
      state: present
  - name: Start docker
    service: 
      name: docker 
      state: started
      enabled: yes
